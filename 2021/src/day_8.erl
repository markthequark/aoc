-module(day_8).

-import(lists, [map/2, seq/2, min/1, max/1, sum/1]).

-compile(export_all).

%% API
-export([p1/1, p2/1]).

%%====================================================================
%% API functions
%%====================================================================

p1(Filename) ->
  solve(Filename).

p2(Filename) ->
  solve(Filename).

%%====================================================================
%% Internal functions
%%====================================================================

solve(Filename) ->
  ok.

